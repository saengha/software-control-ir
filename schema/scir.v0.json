{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://software-control-ir.local/schema/scir.v0.json",
  "title": "Software Control IR v0",
  "description": "Minimal experimental schema for software-facing state, actions, effects, and results.",
  "$defs": {
    "objectId": { "type": "string", "minLength": 1 },
    "json": true,
    "object": {
      "type": "object",
      "required": ["id", "type", "properties"],
      "properties": {
        "id": { "$ref": "#/$defs/objectId" },
        "type": { "type": "string", "minLength": 1 },
        "parent": { "$ref": "#/$defs/objectId" },
        "properties": { "type": "object", "additionalProperties": true }
      },
      "additionalProperties": false
    },
    "state": {
      "type": "object",
      "required": ["revision", "objects", "selection"],
      "properties": {
        "revision": { "type": "integer", "minimum": 0 },
        "objects": { "type": "array", "items": { "$ref": "#/$defs/object" } },
        "selection": { "type": "array", "items": { "$ref": "#/$defs/objectId" } },
        "meta": { "type": "object", "additionalProperties": true }
      },
      "additionalProperties": false
    },
    "action": {
      "type": "object",
      "required": ["action"],
      "properties": {
        "action": { "type": "string", "minLength": 1 },
        "target": { "$ref": "#/$defs/objectId" },
        "params": { "type": "object", "additionalProperties": true }
      },
      "additionalProperties": true
    },
    "effect": {
      "type": "object",
      "required": ["kind", "target"],
      "properties": {
        "kind": { "enum": ["create", "update", "delete", "select"] },
        "target": { "type": "string" },
        "path": { "type": "string" },
        "from": true,
        "to": true
      },
      "additionalProperties": false
    },
    "issue": {
      "type": "object",
      "required": ["code", "message"],
      "properties": {
        "code": { "type": "string" },
        "message": { "type": "string" },
        "path": { "type": "string" }
      },
      "additionalProperties": false
    },
    "result": {
      "type": "object",
      "required": ["status", "action", "revision", "before", "after", "effects"],
      "properties": {
        "status": { "enum": ["accepted", "rejected", "failed"] },
        "action": { "$ref": "#/$defs/action" },
        "revision": { "type": "integer", "minimum": 0 },
        "before": { "$ref": "#/$defs/state" },
        "after": { "$ref": "#/$defs/state" },
        "effects": { "type": "array", "items": { "$ref": "#/$defs/effect" } },
        "issues": { "type": "array", "items": { "$ref": "#/$defs/issue" } },
        "recovery": { "enum": ["compensation", "snapshot"] },
        "focus": {
          "type": "object",
          "properties": {
            "before": { "$ref": "#/$defs/object" },
            "after": { "$ref": "#/$defs/object" }
          },
          "additionalProperties": false
        }
      },
      "additionalProperties": false
    },
    "transaction": {
      "description": "A batch that either lands completely or leaves no changes behind.",
      "type": "object",
      "required": ["status", "revision", "before", "after", "effects", "results", "rolledBack"],
      "properties": {
        "status": { "enum": ["accepted", "rejected", "failed"] },
        "revision": { "type": "integer", "minimum": 0 },
        "before": { "$ref": "#/$defs/state" },
        "after": { "$ref": "#/$defs/state" },
        "effects": { "type": "array", "items": { "$ref": "#/$defs/effect" } },
        "results": { "type": "array", "items": { "$ref": "#/$defs/result" } },
        "rolledBack": { "type": "boolean" },
        "issues": { "type": "array", "items": { "$ref": "#/$defs/issue" } }
      },
      "additionalProperties": false
    },
    "capabilities": {
      "description": "What an adapter can do, so an agent does not have to probe for it.",
      "type": "object",
      "required": ["transactions", "compensation", "relevantState", "hierarchy"],
      "properties": {
        "transactions": { "type": "boolean" },
        "compensation": { "type": "boolean" },
        "relevantState": { "type": "boolean" },
        "hierarchy": { "type": "boolean" }
      },
      "additionalProperties": false
    }
  }
}
